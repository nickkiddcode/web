---
const { pathname } = Astro.url;
const links = [
  { href: '/work', label: 'WORK' },
  { href: '/about', label: 'ABOUT' },
  { href: 'https://brand-storyteller-ai-38203086.base44.app', label: 'Start making', external: true },
];
---

<div id="home" class="header">
  <div class="inner-header">
    <a href="/" aria-current={pathname === '/' ? 'page' : undefined} class:list={['logo', 'w-nav-brand', pathname === '/' && 'w--current']}>
      <span class="logo-image logo-mesh mesh mesh-mask" role="img" aria-label="Nick Kidd logo" style="--mask:url(/assets/665f43a979bbd64b9828e072_nkpurple.png)"></span>
    </a>
    <div class="container-14">
      {links.map((l) => (
        <a
          href={l.href}
          class="nav-button w-inline-block"
          target={l.external ? '_blank' : undefined}
          rel={l.external ? 'noopener' : undefined}
        >
          <div class="navtext">{l.label}</div>
        </a>
      ))}
    </div>
  </div>
</div>
